// SpellsTab.js placeholder
/**
 * SpellsTab module - handles the spells tab content (spellbook, loadouts, etc).
 * Currently a stub that logs its initialization and shows how to access state.
 */
import stateManager from "../../core/StateManager.js";

export default class SpellsTab {
  constructor() {
    // Log initialization and some state data
    console.log(`SpellsTab initialized (${stateManager.spells.length} spells available)`);
    // TODO: Implement spells list, spell loadout configuration, and casting logic here
  }
}
