// FighterTab.js placeholder
/**
 * FighterTab module - handles the fighter (combat) tab content.
 * Currently a stub that logs its initialization and shows how to access state.
 */
import stateManager from "../../core/StateManager.js";

export default class FighterTab {
  constructor() {
    // Log initialization and some state data
    console.log(`FighterTab initialized (Player HP: ${stateManager.player.hp})`);
    // TODO: Implement combat interface, turn-based battle logic, and combat UI here
  }
}
